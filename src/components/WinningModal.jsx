import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const WinningModal = ({player}) => {
  return (
    <View>
      <Text>WinningModal</Text>
    </View>
  )
}

export default WinningModal

const styles = StyleSheet.create({})